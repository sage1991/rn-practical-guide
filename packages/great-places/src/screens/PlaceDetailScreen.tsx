import React, { FC } from "react"
import { View, Text, StyleSheet } from "react-native"


export const PlaceDetailScreen: FC = (props) => {
  return (
    <View>
      <Text>PlaceDetailScreen</Text>
    </View>
  )
}

const styles = StyleSheet.create({})
